module: models.unet.UNet
# DEVICE: cuda
EPOCHS: 5
# BATCH_SIZE: 2
LR: 0.001
momentum: 0.9
# ratio is the number so that above ratio mask = True (for visualization)
# ratio: 0.5 #Various ratios could perform better for visualization
# sample_num: 2
ENCODER: 'resnet50'
WEIGHTS: 'imagenet'
